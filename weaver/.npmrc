# This project uses Bun as the package manager
# If you accidentally run npm/yarn commands, they will be redirected to bun

# Disable npm
prefer-offline=true
audit=false
fund=false

# Note: Please use 'bun install' instead of 'npm install'
# See README.md for Bun-specific commands
